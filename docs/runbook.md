# VaultBank Runbook

{"run":"bash\nstripe listen --forward-to <https://vaultbank.vercel.app/api/payments/webhook/stripe\nstripe> trigger payment_intent.succeeded\n```\n\n- Expected: 200 response from our endpoint","we are back to accepting no live Stripe webhooks\", not \"we are back to working":"--","Idempotency-Key":"uuid>` header on any mutating payment route. The same key within 24h replays the cached response","idempotency":1,"path)":"server/middleware/idempotency.js` caches the response in a TTL-bounded LRU. A duplicate POST within the same Node process returns the cached body immediately.\n2. **Durable (cold-start backstop):** the `payments` table has a `idempotency_key` column; the transfer route looks it up via `ledger.findPaymentByIdempotencyKey()` and replays the original record. This survives Vercel cold starts and process restarts.\n\nWebhook deduplication is a **separate** mechanism using `webhook_events(provider","https":"vaultbank.vercel.app/api/payments/transfer \n    -H","Authorization":"Bearer <JWT>","Content-Type: application/json\" \n    -H":"dempotency-Key: $KEY","{\"fromAccountId\":\"acct1\",\"to\":\"acct2\",\"amount\":10,\"currency\":\"USD\",\"method\":\"stripe_card\"}":"Repeat -- the second response will have the `Idempotent-Replay: true` header\n```\n\n### How to roll back\n\n- Stop sending the header. The middleware is a no-op without the header.\n\n---\n\n## 3. Production-safety env validation\n\n### What changed\n\n`server/payments/safety.js` blocks `NODE_ENV=production` from accepting mock provider keys. If you try to use the `stripe` adapter in production without a real `PAYMENT_PROVIDER_STRIPE_SECRET`","require('./server/payments/safety').validateProductionConfig({silent:true})":"on every PR and fails the build if any required env var is still a placeholder.\n\n### How to disable (NEVER in real-money production)\n\nSet `ALLOW_MOCK_IN_PRODUCTION=true`. This logs a loud warning at startup and bypasses the check. Use only for staging deploys without real keys.\n\n### How to roll back\n\n- Set `ALLOW_MOCK_IN_PRODUCTION=true` to bypass (NOT recommended).\n- Or fix the missing env vars in your Vercel / GitHub Secrets config.\n\n---\n\n## Common diagnostic queries\n\n```sql\n-- recent webhook events with errors\nSELECT provider","failed' OR error IS NOT NULL\nORDER BY received_at DESC LIMIT 50;\n\n-- payments waiting on a webhook (status=pending and no webhook event for them)\nSELECT p.id, p.provider, p.provider_id, p.amount, p.currency, p.status, p.created_at\nFROM payments p\nLEFT JOIN webhook_events w\n  ON w.provider = p.provider AND w.provider_event_id = p.provider_id\nWHERE p.status = 'pending'\n  AND p.created_at < now() - interval '15 minutes'\n  AND w.id IS NULL\nORDER BY p.created_at DESC;\n\n-- replay rate (idempotency-key hits)\nSELECT\n  count(*) FILTER (WHERE metadata->>'replay' IS NOT NULL) AS replays,\n  count(*) AS total\nFROM payments\nWHERE created_at > now() - interval '24 hours":"--"}
