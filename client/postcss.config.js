
module.exports = {
  plugins: {
    // Use the dedicated Tailwind CSS PostCSS plugin as required by Tailwind v4+.
    // This replaces the direct usage of `tailwindcss` which is no longer supported.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
